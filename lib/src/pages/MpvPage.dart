// Packages
import 'package:flutter/material.dart';

class MpvPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("MPV", style: TextStyle(fontWeight: FontWeight.bold, fontSize: 30),));
  }
}