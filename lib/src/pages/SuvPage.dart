// Packages
import 'package:flutter/material.dart';

class SuvPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("SUV", style: TextStyle(fontWeight: FontWeight.bold, fontSize: 30),));
  }
}